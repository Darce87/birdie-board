# Birdie Board

A mobile-first golf tournament scorer for Stableford competitions. It supports competitor handicaps, course stroke indices, live points, and scorecard photo import with browser-based OCR.

## GitHub Pages

This is a static website. In GitHub, open **Settings → Pages**, choose **Deploy from a branch**, then select the `main` branch and the `/ (root)` folder.
